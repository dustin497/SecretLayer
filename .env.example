# API
PORT=8787
API_ORIGIN=http://localhost:8787
WEB_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-production
APP_VERSION=0.2.0

# Stripe billing (API server only — Railway in production)
# Secret key starts with sk_test_ or sk_live_
STRIPE_SECRET_KEY=
# Webhook signing secret starts with whsec_ (from Stripe Dashboard → Webhooks)
STRIPE_WEBHOOK_SECRET=
# Price IDs from Stripe Dashboard → Products (price_...)
STRIPE_PRICE_ID_PERSONAL=
STRIPE_PRICE_ID_PRO=

# Promotion (optional — used when checks pass)
NETLIFY_AUTH_TOKEN=
NETLIFY_SITE_ID=
PROMOTION_WEBHOOK_URL=
